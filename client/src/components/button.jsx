function Button() {
  //on button click
  //fetch user
  //if user- fetch role and check role on step
  //if new user select role and store user
  return (
    <div className="mt-20">
      <button className="bg-slate-300 p-3 rounded-md">Fetch User</button>
    </div>
  );
}

export default Button;
